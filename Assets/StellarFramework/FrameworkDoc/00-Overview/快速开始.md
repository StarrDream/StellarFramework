# StellarFramework 快速开始

这份文档只负责让新人先跑通框架。深度功能说明请进入 Kit 说明文档；源码实现请进入对应源码文档。

## 第一次打开项目

1. 打开 Unity 菜单 `StellarFramework -> Tools Hub`。
2. 在左侧进入 `Start Here -> Quick Start`。
3. 打开 `Assets/StellarFramework/Samples/ArchitectureDemo/Scene/FrameworkArchitecture_Playable.unity`。
4. 运行 Demo，点击 Mine，确认 Model / Service / Bindable / UI 的真实数据流。
5. 切换 `中文 / English`，确认 LocalizationKit 与 UI 联动。
6. 再根据项目需求进入对应 Kit Guide；需要内容热更新时先接入 YooAsset，需要代码热更新时进入 `热更新 -> HybridCLR DLL 导出`。

## 新项目默认选择

- 架构：用 `Architecture<T>` 管 Model、Service、View。
- UI：只从 `UIKit` 进入，不在业务层直接散落 Instantiate UI Prefab。
- 资源：只从 `ResKit` 分配 loader，不让业务直接关心 Resources、AB、AA。
- 内容热更：这是可选能力。生产项目推荐由 YooAsset 负责 Package 版本、Manifest、下载与缓存，业务资源访问仍统一经过 ResKit。
- 代码热更：这是可选能力。导入 `HybridCLRKit` 后，运行时通过 ResKit 读取 `HotUpdateManifest.json`、DLL 和 AOT metadata，再进入 HybridCLR 热更程序集。
- 配置、设置、音频、事件：按 `ConfigKit`、`SettingsKit`、`AudioKit`、`EventKit` 的说明文档接入。

## 架构最小模板

```csharp
using StellarFramework;
using StellarFramework.Bindable;
using UnityEngine;

public sealed class GameApp : Architecture<GameApp>
{
    protected override void InitModules()
    {
        RegisterModel(new PlayerModel());
        RegisterService(new PlayerService());
    }
}

public sealed class PlayerModel : AbstractModel
{
    public readonly BindableProperty<int> Hp = new BindableProperty<int>(100);
}

public sealed class PlayerService : AbstractService
{
    public void TakeDamage(int damage)
    {
        PlayerModel model = GetModel<PlayerModel>();
        model.Hp.Value = Mathf.Max(0, model.Hp.Value - damage);
    }
}

public sealed class GameEntry : MonoBehaviour
{
    private void Awake()
    {
        GameApp.Interface.Init();
    }
}
```

View 只读 Model、调用 Service：

```csharp
using StellarFramework;
using UnityEngine;

public sealed class PlayerHudView : StellarView
{
    public override IArchitecture Architecture => GameApp.Interface;

    public override void OnBind()
    {
        this.GetModel<PlayerModel>().Hp.RegisterWithInitValue(OnHpChanged)
            .UnRegisterWhenGameObjectDestroyed(gameObject);
    }

    public void OnClickDamage()
    {
        this.GetService<PlayerService>().TakeDamage(10);
    }

    private void OnHpChanged(int hp)
    {
        Debug.Log($"当前血量: {hp}");
    }

    public override void OnUnbind()
    {
    }
}
```

继续阅读：[架构说明文档](../01-Architecture/Architecture/Architecture-MSV-架构说明文档-Guide.md) 和 [架构源码文档](../01-Architecture/Architecture/Architecture-MSV-架构源码文档-Guide.md)。

## UIKit 最小模板

```csharp
using Cysharp.Threading.Tasks;
using StellarFramework.UI;
using UnityEngine;

public sealed class LoginPanelData : UIPanelDataBase
{
    public string DefaultAccount;
}

public sealed class LoginPanel : UIPanelBase
{
    public override void OnOpen(UIPanelDataBase data)
    {
        if (TryGetPanelData<LoginPanelData>(data, out LoginPanelData loginData))
        {
            Debug.Log(loginData.DefaultAccount);
        }
    }
}

public sealed class UIEntry : MonoBehaviour
{
    private async UniTaskVoid Start()
    {
        await UIKit.Instance.InitAsync();
        await UIKit.OpenAsync<LoginPanel>(new LoginPanelData { DefaultAccount = "player01" });
    }
}
```

继续阅读：[UIKit 说明文档](../02-Kits/UIKit/UIKit-界面系统-说明文档-Guide.md) 和 [UIKit 源码文档](../02-Kits/UIKit/UIKit-界面系统-源码文档-Guide.md)。

## ResKit 最小模板

```csharp
using Cysharp.Threading.Tasks;
using StellarFramework.Res;
using UnityEngine;

public sealed class PrefabSpawner : MonoBehaviour
{
    private IResLoader _loader;
    private const string PrefabPath = "Assets/Game/Prefabs/Hero.prefab";

    private async UniTaskVoid Start()
    {
        _loader = ResKit.Allocate(ResLoaderRequest.Custom("Addressables", nameof(PrefabSpawner)));
        GameObject prefab = await _loader.LoadAsync<GameObject>(PrefabPath, destroyCancellationToken);

        if (prefab != null)
        {
            Instantiate(prefab);
        }
    }

    private void OnDestroy()
    {
        if (_loader == null)
        {
            return;
        }

        _loader.Unload(PrefabPath);
        ResKit.Recycle(_loader);
        _loader = null;
    }
}
```

后端选择：

```csharp
IResLoader resources = ResKit.Allocate(ResLoadBackend.Resources, "Local");
IResLoader aa = ResKit.Allocate(ResLoaderRequest.Custom("Addressables", "RemoteAA"));
IResLoader ab = ResKit.Allocate(ResLoadBackend.AssetBundle, "LegacyAB");

ResKit.RegisterCustomLoader("YooAsset", request => new YooAssetResLoader());
IResLoader custom = ResKit.Allocate(ResLoaderRequest.Custom("YooAsset", "ThirdParty"));
```

继续阅读：[ResKit 说明文档](../02-Kits/Reskit/ResKit-统一资源-说明文档-Guide.md) 和 [ResKit 源码文档](../02-Kits/Reskit/ResKit-统一资源-源码文档-Guide.md)。

## 热更新最小路线

StellarFramework 把“内容热更新”和“代码热更新”分开：

1. 项目启动层初始化 YooAsset ResourcePackage。
2. 由 YooAsset 完成版本请求、Package Manifest 更新、下载和缓存。
3. 调用 `YooAssetResKitInstaller.Install(...)`，让 ResKit 可以读取已经准备好的内容。
4. 调用 `HybridCLRKit.RunAsync()`。
5. HybridCLRKit 通过同一个 ResKit 后端读取 `HotUpdateManifest.json`、热更 DLL 和 AOT metadata。
6. 校验 DLL SHA256，加载 AOT metadata，再进入热更程序集。

Addressables 在框架中只作为 ResKit 的可选 Load / Release 后端与本地 Player Content 构建入口，不再承担 StellarFramework 的正式资源热更新工作流。

继续阅读：[HybridCLRKit 说明文档](../02-Kits/HybridCLRKit/HybridCLRKit-代码热更新-说明文档-Guide.md) 和 [HybridCLRKit 源码文档](../02-Kits/HybridCLRKit/HybridCLRKit-代码热更新-源码文档-Guide.md)。

## 常见排错

- ArchitectureDemo 缺失：检查 `Assets/StellarFramework/Samples/ArchitectureDemo` 是否完整导入。
- UI 打不开：检查 `UIKitSettings`、`UIRoot`、面板 Prefab 路径和加载后端。
- Addressables 同步加载返回空：生产 AA 使用 `LoadAsync<T>`。
- AB 加载失败：先运行 ToolsHub 的 `资源打包 (AssetBundle)`，再确认 `AssetBundleManager.InitAsync()` 已执行。
- 内容更新没生效：先检查 YooAsset Package 初始化、版本、Manifest 与 Downloader 是否完成，再确认对应 ResKit Loader 已注册。
- DLL SHA256 mismatch：重新执行 HybridCLR DLL 导出，并确保 Manifest、DLL、metadata 被同一个内容版本一起发布。

## 下一步阅读

- [唯一 Demo 索引](../03-Samples/Samples_Index.md)
- [ToolsHub 说明文档](../04-ToolsHub/StellarToolsHub-说明文档-Guide.md)
- [ResKit 说明文档](../02-Kits/Reskit/ResKit-统一资源-说明文档-Guide.md)
- [HybridCLRKit 说明文档](../02-Kits/HybridCLRKit/HybridCLRKit-代码热更新-说明文档-Guide.md)
- [框架源码阅读入口：架构源码文档](../01-Architecture/Architecture/Architecture-MSV-架构源码文档-Guide.md)

## 生产热更新规则

- 编辑器和 Development Build 允许开发期诊断；正式发布构建会启用严格的 DLL SHA256 校验。
- `HotUpdateManifest.json`、`HotUpdate.dll.bytes` 和全部 AOT metadata 必须由同一个 ResKit 内容版本提供。
- 每次代码热更发布都先通过 Tools Hub 重新导出 DLL、metadata 与 Manifest，再交给 YooAsset 内容管线一起发布。
- 生产放行前必须在目标平台的 IL2CPP Player 上完成真实内容更新、SHA 校验、metadata 加载、热更入口执行和失败/回滚验证；编辑器检查不能替代这一步。
